using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using System.IO;
using System.Reflection.Emit;
using UnityEngine.SceneManagement;
using System;

public class GameCreatorManager : MonoBehaviour
{
	/*public InputField input;
	private string fileName;

	void OnMouseDown()
	{
		fileName = input.text;
		UpdateFile (fileName);
	}

	public void UpdateFile(string fileName)
	{
		if (File.Exists (fileName)) {
			Console.WriteLine ("Cannot create file with file name " + fileName + "because file already exists!");
			return;
		} else {
			GameManager gm = new GameManager ();
			System.IO.File.WriteAllText ("C:\\Users\\Alex\\Documents\\Galaxy Clicker\\Assets\\Saves", 1 + ", " + 0 + ", " + 0);
		}
	}

	void OnGUI()
	{
		
	}
	*/

}

